1. Agent take same system's message for two times call "extract system_message" --> make the call_user_simulator is working slightly incorectly
2. I want flexible control custom memory_interval for trigger done a turn chat instead of assign a number of steps.

3. See the run with ``planner_model` to better performance.

For issue #2 and #3: https://docs.browser-use.com/customize/agent-settings#memory-management